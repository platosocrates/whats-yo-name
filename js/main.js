var newName = prompt("What is your name?");

if (newName == "Thomas") {
    alert("Hey " + newName + ", our names are the same!" );
} else {
    alert("Too bad our names are different, " + newName + ".");
}